{
    "name": "chaind",
  "version": "1.0.0",
  "type": "module",
  "description": "Permanent jewelry",
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd ../backend && npm install"
    }
}